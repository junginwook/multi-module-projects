
bootJar.enabled = true
jar.enabled = false

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

bootJar {
    launchScript()
    manifest {
        attributes 'Start-Class': 'com.inwook.project.api.ModuleApiApplication'
    }
}
